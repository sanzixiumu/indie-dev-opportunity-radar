# 环境搭建

日期：2026-06-01  
包管理器：pnpm  
临时 CLI：bunx  
微信开发者工具免费开发环境 EnvId：`cloud1-d8gh1a3icf3f03932`

## 当前状态

- 已安装 CloudBase skill 到 `.agents/skills/cloudbase`。
- 已配置 CloudBase MCP：
  - `.mcp.json`
  - `config/mcporter.json`
- 微信开发者工具中当前使用免费开发环境：`cloud1-d8gh1a3icf3f03932`。
- 当前小程序 AppID：`wxf36d952b4427bb2f`。
- 小程序运行时 `wx.cloud.init` 使用：`cloud1-d8gh1a3icf3f03932`。
- 已完成 CloudBase MCP 设备码授权。
- MCP 当前登录状态：READY。
- 注意：MCP 之前自动绑定的 `indiedevopportunityrader-df7df79` 是腾讯云侧体验环境，不是当前微信开发者工具里的免费开发环境。当前小程序开发以 `cloud1-d8gh1a3icf3f03932` 为准。

## 本项目命令约定

后续不使用 npm。

- 项目依赖使用 `pnpm`。
- 临时运行 CloudBase CLI 使用 `bunx @cloudbase/cli`。
- 临时运行 mcporter 使用 `bunx mcporter`。

## 环境检查命令

```bash
node -v
pnpm -v
bun -v
```

当前已验证：

```text
node v25.2.1
pnpm 10.14.0
bun 1.3.13
```

## CloudBase MCP 命令

查询登录状态：

```bash
bunx mcporter call cloudbase.auth action=status --output json
```

查询环境列表：

```bash
bunx mcporter call cloudbase.envQuery action=list --output json
```

查看 CloudBase MCP 工具参数：

```bash
bunx mcporter describe cloudbase --all-parameters
```

## CloudBase CLI 命令

查看版本：

```bash
bunx @cloudbase/cli --version
```

当前已验证：

```text
CloudBase CLI 3.5.3
```

## 微信开发者工具下一步

1. 打开微信开发者工具。
2. 使用你的微信账号登录。
3. 选择“小程序”。
4. 如果已有小程序 AppID，使用真实 AppID 导入项目。
5. 如果暂时没有 AppID，可以先使用测试号，但后续支付、发布、真实云开发能力需要真实 AppID。
6. 进入“云开发 / CloudBase”，确认当前环境为 `cloud1-d8gh1a3icf3f03932`。
7. 如果切换环境，必须同步修改 `app.js` 里的 `wx.cloud.init({ env })`。

## 后续创建小程序工程建议

第一版建议使用微信原生小程序，目录规划如下：

```text
indie-dev-opportunity-radar/
  docs/
  miniprogram/
  cloudfunctions/
  config/
  .env.example
  package.json
```

后续创建小程序端后，在 `miniprogram` 目录使用 pnpm 安装依赖：

```bash
cd miniprogram
pnpm init
pnpm add tdesign-miniprogram dayjs
pnpm add -D miniprogram-ci typescript prettier eslint
```

安装后，在微信开发者工具中执行：

```text
工具 -> 构建 npm
```

## 后续创建云函数依赖建议

每个云函数目录独立安装依赖，使用 pnpm：

```bash
cd cloudfunctions/recommendProject
pnpm init
pnpm add wx-server-sdk openai zod axios dotenv dayjs nanoid
pnpm add -D typescript @types/node
```

## 开发前必须确认

- 已有微信小程序 AppID。
- 微信开发者工具能打开项目。
- CloudBase 环境 ID 已填入小程序 `wx.cloud.init`。
- `.env` 已从 `.env.example` 复制，并填入必要 Key。
- AI Key 不写入前端，不提交到 Git。

## 官方快速开始要点

CloudBase 快速开始的核心顺序是：

1. 开通 CloudBase 环境。
2. 按应用类型接入 SDK。
3. 小程序端初始化 `wx.cloud.init`。
4. 使用云函数、数据库、存储等云开发能力。
5. 通过控制台、微信开发者工具、CLI 或 MCP 管理环境资源。

本项目当前已完成第 1 步和管理通道配置，下一步是创建小程序工程并接入 `wx.cloud.init`。
